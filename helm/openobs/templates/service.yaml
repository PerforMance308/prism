apiVersion: v1
kind: Service
metadata:
  name: {{ include "openobs.fullname" . }}
  labels:
    {{- include "openobs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "openobs.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
